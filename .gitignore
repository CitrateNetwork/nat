/target
**/*.rs.bk
Cargo.lock.orig
.DS_Store
# Local secrets / runtime state — NEVER commit
.keys/
*.sock
/data/
/run/
# Model artifacts and corpora are huge and licensed; they never live in git.
*.gguf
*.onnx
*.safetensors
/weights/
/corpus/
# TLC model-checking scratch output
formal/states/
formal/*.toolbox/

# TLC tools (downloaded)
/.tlc/

# LaTeX build artifacts
paper/arxiv/*.pdf
paper/arxiv/*.aux
paper/arxiv/*.bbl
paper/arxiv/*.blg
paper/arxiv/*.log
paper/arxiv/*.out

# ─────────────────────────────────────────────────────────────────────
# Open-source hygiene — secret material & internal records (added 2026-09)
# History was scrubbed of these; this block prevents re-introduction.
# ─────────────────────────────────────────────────────────────────────

# Secret material — never in a public repo
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.p8
*.der
id_rsa
id_ed25519
*_private*.pem
*private_key*
*jwks*.json
**/secrets.json
**/secrets.yaml
**/secrets.yml
**/secrets.toml
mnemonic*
*seed-phrase*
*seed_phrase*

# Internal process / institutional-knowledge records — not for public repos
.agentile/
handoffs/
**/*HANDOFF*
**/*_HANDOFF*
**/*INCIDENT*
**/*EXEC_STATUS*
**/*COUNSEL*
**/*DEPLOY_LOG*
